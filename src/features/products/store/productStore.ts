// Product type definition - kept for backward compatibility
// All product operations are now handled by TanStack Query hooks in useProducts.ts
// Re-export TransformedProduct as Product for backward compatibility
export type { TransformedProduct as Product } from '../interfaces/product';
